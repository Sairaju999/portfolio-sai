{
  "name": "sai-raju",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --root \"sai raju\"",
    "build": "vite build --root \"sai raju\" --outDir dist",
    "preview": "vite preview --root \"sai raju\"",
    "start": "vite preview --root \"sai raju\" --host 0.0.0.0 --port $PORT"
  },
  "devDependencies": {
    "vite": "^8.1.1"
  }
}
